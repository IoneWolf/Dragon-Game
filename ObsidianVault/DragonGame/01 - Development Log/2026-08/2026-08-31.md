# 2026-08-31 - Game Loop Foundations

## Completed

- Fixed the crouch pose appearing to float above the ground before falling back into place.
- Moved the `SpriteRenderer` and `PlayerSpriteVisual` from the physics root to a `Visual` child on `Player`.
- Anchored the crouch visual to its bottom edge: it halves in height while shifting downward by the matching half-height difference.
- Kept the root `Rigidbody2D` and `BoxCollider2D` unchanged while crouching, preserving stable floor contact and ground detection.
- Repaired a prefab component-ownership issue found during the change, which had briefly caused the root physics transform to scale and trigger fall respawn on crouch.
- Changed level exits from `E` interactions to proximity triggers. On entry, the player ignores normal input, walks right at a reduced speed for `0.6` seconds, then loads the configured destination scene.
- Made the transition walk directional: forward exits walk and face right, while reverse exits walk and face left.
- Removed the unused interaction prompt from the reusable `LevelExit` prefab.
- Prevented additive scene transitions from creating duplicate global lights by rejecting already-loaded destination scenes.
- Prevented temporary destination players from competing with the retained player for `PlayerInput` devices during scene transitions.
- Added Inspector tooltips to all editable project-authored component and NPC dialogue asset fields, documenting expected inputs, units, layer masks, scene/spawn formats, and fallback behavior.
- Created `PlayersHouse.unity` with an empty 1x1 Grid and collision-ready TileMap: `TilemapCollider2D`, `CompositeCollider2D`, static `Rigidbody2D`, and the shared no-friction physics material. Added a disabled scene camera and enabled the scene in Build Settings for a future explicit transition.
- Updated the main menu: `Play` now loads `PlayersHouse`, while `Playtest` loads Level 1 directly.
- Organized scene assets: `Persistent` and `MainMenu` remain at the scene root; Levels 1-3 are under `Feature Sandbox` for development testing, and `PlayersHouse` is under `Chapter 1`. Updated all menu, controller, and Build Settings paths.
- Added the scene-owned Player prefab to `PlayersHouse` at its existing spawn marker. `Persistent` remains responsible only for global systems; the first gameplay scene supplies the initial Player.
- Added `PlatformDarkGray`, a paintable one-way platform tile, and a `One Way Platforms` TileMap in `PlayersHouse`. The player can drop through these platforms by crouching or double-tapping `S` while standing on one.
- Repaired one-way platform drop-through detection by using the player's live collider contacts rather than a layer touch query, then temporarily ignoring the contacted platform collider.
- Added a `Characters` sorting layer and assigned the player visual to it, ensuring the player renders in front of all default-world TileMaps.
- Added `CameraBoundsCorner.prefab`. Place `Bottom Left` and `Top Right` marker instances at a map's playable corners to clamp the full camera viewport inside the authored level.
- Reworked game-over restart to retain the current Player, restore full health, and place it at the nearest spawn point in the active scene instead of unloading and recreating the level player.
- Added a Global Light 2D to `PlayersHouse` so URP lit sprites, including the Player, render with their configured colors rather than black.
- Made `PlayerSpriteVisual` explicitly apply its configured blue color to the renderer on every refresh, preventing scene or material state from tinting the player black.
- Replaced the procedural blue-square player art with a looping two-frame dragon idle animation using `DragonIdle1.png` and `DragonIdle2.png`, imported as 32 PPU point-filtered sprites.
- Doubled the rendered dragon visual size and raised it by `0.5` world units without changing player physics or collision.

## Validation

- Unity EditMode batch tests completed without reported errors.
- C# editor diagnostics were clean for the edited scripts and documentation.
- Verified permanent documentation for player controls/animation, one-way platforms, camera bounds, scene transitions, main-menu routes, scene hierarchy, and restart behavior.

Related: [[Player]]