# 2026-08-29

## Player Movement: Wall Sticking
- Investigated the player sticking to walls while holding left/right into a wall.
- Decided the first fix should be a zero-friction `PhysicsMaterial2D`, not a new wall layer or wall script.
- Existing editor tool: **Tools -> Dragon Adventure -> Generate No-Friction Physics Material 2D** creates `Assets/Physics/NoFriction.physicsMaterial2D`.
- Apply `NoFriction.physicsMaterial2D` to the Player's `Collider2D` Material field and the environment collider's Material field (`TilemapCollider2D` or `CompositeCollider2D` if the tilemap uses one).
- A wall layer is only needed later for wall-specific gameplay logic like wall sliding, wall jumping, ledge grabbing, or enemy wall checks.
- If sticking or upward popping still happens after applying the material, check `PlayerController.HandleSnag`: the anti-snag nudge may need to ignore cases where the player is pushing into a real wall.

## Camera: Vertical Limits
- Added optional vertical camera limits to `CameraFollow`.
- New inspector fields: `useVerticalLimits`, `topLimit`, and `bottomLimit`.
- When enabled, the camera follows the player normally on X, but clamps its Y center between the bottom and top values before smoothing.

## Music: Startup Playback
- Added `Assets/Resources/Music` as the project music folder.
- Added `GameMusicPlayer`, a startup/background music component for a normal scene object.
- If a `startupMusic` clip is assigned in the Inspector, it plays that clip; otherwise it loads the first `AudioClip` from `Assets/Resources/Music`.
- Music loops by default, uses adjustable volume, and persists between scenes by default.
- Moved `Soundtrack derg game.mp3` into `Assets/Resources/Music` so it auto-plays when Level 1 starts.
- Removed the editor menu creator after assembly-reference issues; the preferred setup is now a manually created scene object with `GameMusicPlayer` attached.
- Added an Add Component menu label for `GameMusicPlayer` so it can be found under **Dragon Adventure/Audio/Game Music Player**.
- Added explicit startup loading for `Assets/Resources/Music/Soundtrack derg game.mp3` before falling back to any other music clip in the folder.
- Updated the soundtrack import settings to preload audio data and import as 2D audio.
- Final setup uses a manually placed `GameMusicPlayer` object in `Level 1.unity` with an `AudioSource` referencing `Soundtrack derg game.mp3`.

## Interaction Prompt Cleanup
- Added `IInteractionPrompt` as an optional companion interface for interactables that can show/hide a prompt.
- Added `InteractionPromptIcon`, a reusable floating `TextMesh` prompt for interactables.
- Updated `PlayerInteractor` to show the prompt on the nearest interactable in range and hide prompts while dialogue is open.
- Updated `RockInteractable` to require `InteractionPromptIcon`, and added a `Rock Interactable` prefab.

## Git Ignore Cleanup
- Updated `.gitignore` to ignore Unity `.meta` files going forward, per project preference.

## NPC: Placeholder Dialogue Character
- Added `NPCVisual`, a yellow square placeholder sprite component for NPCs.
- Added `NPCInteractable`, which plugs into the existing `IInteractable` / `PlayerInteractor` flow and opens `DialogueUI`.
- Added `NPCDialogueData` as a separate `ScriptableObject` asset type for writing NPC dialogue outside the interaction script.
- Created starter dialogue asset: `Assets/Dialogue/Friendly NPC Dialogue.asset`.
- Documented functionality, setup, dialogue-data workflow, and current limits in [[NPC]], with cross-links from [[Interaction System]] and [[Dialogue System]].

## Player: Fall Respawn
- Added configurable fall respawn to `PlayerController`.
- The controller records the last grounded physics position.
- If the player falls below `fallRespawnY`, it respawns them at that saved position plus `respawnXOffset` / `respawnYOffset` and clears velocity.
- Inspector fields: `useFallRespawn`, `fallRespawnY`, `respawnXOffset`, and `respawnYOffset`.
- Default `respawnXOffset` is `1`.
- Fixed the X offset so it applies opposite the player's last movement direction instead of always pushing right, preventing respawns from being nudged back into a pit from one side.

## Level Transitions
- Added `LevelExit`, an `IInteractable` for exits/doors/area edges that load a target scene when the player presses Interact nearby.
- Added `LevelSpawnPoint`, which places the player at a matching `spawnId` after a scene transition.
- Scene transition state is now owned by `GameController` instead of a standalone static helper.
- `LevelExit` defaults its prompt text to `Press E to proceed`.
- Updated `LevelExit` to keep the existing player alive across the scene load.
- Updated `LevelSpawnPoint` to reconnect the target scene's main camera to the moved player.
- Wired `Level 1`'s `LevelExit` to load `Level 2` with `targetSpawnId = FromLeft`.
- Added a matching `Spawn From Left` `LevelSpawnPoint` to `Level 2`.
- Added `Level 2` to Unity Build Settings.
- Changed `LevelExit` so reusable prefabs can load the next/previous scene based on Build Settings order instead of hardcoding one target scene name.
- Cleaned `LevelExit.prefab` to be a generic forward exit using `exitMode = NextLevel`.
- Added a `LevelExit Back` object in `Level 2` using `exitMode = PreviousLevel` and a matching `Spawn From Right` point in `Level 1`.
- Documented setup and current limits in [[Level Transitions]].

## Game Controller
- Added `GameController` as the persistent owner for scene transition requests and simple run state.
- Added `GameController.levelSceneNames` so level order is serialized in the Inspector instead of being hardcoded into each exit prefab.
- `LevelExit` now asks `GameController` to load next/previous/explicit scenes instead of calling `SceneManager` directly.
- `LevelSpawnPoint` now asks `GameController` whether its `spawnId` matches the pending transition spawn.
- Removed the separate `LevelTransitionState` helper because `GameController` now owns pending spawn data.
- Added a persistent `score` field plus `AddScore` / `ResetScore` methods as the starting point for cross-scene game state.
- Documented setup in [[Game Controller]].
