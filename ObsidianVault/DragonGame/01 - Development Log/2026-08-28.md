# 2026-08-28

## Git
- Installed Git for Windows and linked the project to `https://github.com/IoneWolf/Dragon-Game.git`.
- Added a Unity-appropriate `.gitignore` (scoped to `Dragon Adventures/`) and pushed the initial commit.

## Obsidian Vault
- Reorganized the vault from a flat file list into the numbered folder structure (`00 - Project`, `01 - Development Log`, `02 - Systems`, `09 - Assets`, `99 - Archive`).
- Split the old `README.md` into [[Game Overview]], [[Development Roadmap]], and [[Asset Requirements]]; archived the legacy `TileGrid` write-up into [[TileGrid (Legacy System)]].

## New Feature: Interaction + Dialogue System
- Added an `Interact` input (tap `E`, was defaulted to a `Hold` interaction) and a generic `IInteractable` contract.
- `PlayerInteractor` finds the nearest interactable in range and triggers it; ignores Interact while a dialogue is open.
- Built a reusable `DialogueUI` (speaker sprite + text + choice buttons), full-width bar anchored to the bottom of the screen.
- `PlayerController` now freezes horizontal movement/jumping while `DialogueUI.IsOpen` (gravity still applies).
- Added the first example interactable: a talking rock (`RockVisual` + `RockInteractable`, placeholder half-circle sprite) — "Pick me up" → Pick it up? / Leave it? → picking it up plays a reaction line and removes the rock.
- Documented as [[Interaction System]] and [[Dialogue System]].
