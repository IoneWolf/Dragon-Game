# Music

Put startup/background music AudioClip files here (`.wav`, `.mp3`, or `.ogg`).

`GameMusicPlayer` auto-loads the first clip in this folder when the game starts if no specific `startupMusic` clip is assigned in the Inspector.